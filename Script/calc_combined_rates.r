# calc_combined_rates.r

calc_combined_rates <- function( m_tisNA )
{
   if ( FALSE ) { 
     celltype <- "B"
     source( "universal_script_setup.r" )
     source( "testing_model_setup.r", local = TRUE ) 
   } else {
     celltype <- m_tisNA$celltype
   }
  
  
  
}